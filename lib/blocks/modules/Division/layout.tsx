// lib/blocks/modules/Division/layout.tsx
// C 객체: Division 블록의 레이아웃 렌더링

import React from 'react';
import { BlockInstance } from '../../BlockInstance';
import { DivisionBlockLayoutRenderer } from '../../layout/DivisionBlockLayoutRenderer';
import { RenderCellContext } from '../../layout/BlockLayoutRenderer';

/**
 * Division 블록의 레이아웃 렌더러
 * DivisionBlockLayoutRenderer를 상속하여 사용
 */
export class DivisionLayoutRenderer extends DivisionBlockLayoutRenderer {
  // Division 특화 로직이 필요하면 여기에 추가
}

